
using ECS.Components.AI;

namespace ECS.Systems.Attacking
{
    /// <summary>
    /// This class will hold definitions for how to handle each attack. 
    /// This might explode, but it should enable very unique behavior for
    /// each different attack that a character has. It'll just be a bit of work.
    /// </summary>
    public static class GenericAttackHandling
    {
        /// <summary>
        /// Generic jab for basically every character. 
        /// </summary>
        /// <param name="stats"></param>
        public static void HandleJab(AttackStats stats)
        {

        }

    }
}
